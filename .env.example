# Copy this file to .env and fill in values as needed.
#
# Expo inlines any EXPO_PUBLIC_ prefixed variable into the client bundle at
# build time — no extra library required (see 00-tech-stack.md Library
# Policy: "prefer platform/native capability over a third-party package").
# Because these are bundled into the client, EXPO_PUBLIC_ vars must never
# hold secrets (11-coding-standards.md "No secrets inside source code").
#
# Firebase client configuration is optional. Parent Auth is unavailable when
# the required values are absent; child exploration and local SQLite continue.
# These are client-visible identifiers, never Admin SDK/service-account secrets.

EXPO_PUBLIC_APP_ENV=development

# ---------------------------------------------------------------------------
# Firebase — optional parent Auth foundation (Phase 9C)
# ---------------------------------------------------------------------------
# Register a Firebase web app in the development/staging/production Firebase
# project that matches EXPO_PUBLIC_APP_ENV. Do not point a production build at
# a development project. Firebase Auth uses AsyncStorage internally for its
# own session persistence; this app does not manually persist Auth tokens.
EXPO_PUBLIC_FIREBASE_API_KEY=
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=
EXPO_PUBLIC_FIREBASE_PROJECT_ID=
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
EXPO_PUBLIC_FIREBASE_APP_ID=

# ---------------------------------------------------------------------------
# Development-only native validation harnesses (never enable in a release)
# ---------------------------------------------------------------------------
# EXPO_PUBLIC_PHASE9_SYNC_QUEUE_VALIDATION=true
# EXPO_PUBLIC_PHASE9_FIRESTORE_VALIDATION=true
# EXPO_PUBLIC_PHASE9_BACKUP_VALIDATION=true
# EXPO_PUBLIC_PHASE9_MULTIDEVICE_VALIDATION=true

# Firebase Emulator Suite is permitted only in __DEV__ builds and only when
# explicitly enabled. Use 10.0.2.2 for an Android emulator reaching host ports.
# EXPO_PUBLIC_FIREBASE_USE_EMULATOR=true
# EXPO_PUBLIC_FIREBASE_EMULATOR_HOST=10.0.2.2
# EXPO_PUBLIC_FIREBASE_AUTH_EMULATOR_PORT=19099
# EXPO_PUBLIC_FIREBASE_FIRESTORE_EMULATOR_PORT=18080
# EXPO_PUBLIC_FIREBASE_FUNCTIONS_EMULATOR_PORT=15001
# EXPO_PUBLIC_FIREBASE_FUNCTIONS_REGION=us-central1

# ---------------------------------------------------------------------------
# Content source
# ---------------------------------------------------------------------------
# 'mock'   — bundled content in services/mock/ (default). Works offline, with
#            no .env and no published content.
# 'sanity' — live content from the Studio in ../studio-factnuggets.
#
# ⚠️ 'sanity' currently reads straight from the CDN. The SQLite offline cache
# (06-content-sync-engine.md) is not built yet, so this is a development mode,
# not a shippable one.
EXPO_PUBLIC_CONTENT_SOURCE=mock

# ---------------------------------------------------------------------------
# Sanity
# ---------------------------------------------------------------------------
# Safe to expose: the project id and dataset name identify a *public, read-only*
# CDN endpoint. They are not credentials. Never add a Sanity write token here —
# an EXPO_PUBLIC_ token ships inside the app bundle and can be extracted from
# any installed copy.
EXPO_PUBLIC_SANITY_PROJECT_ID=bhgo6qa7
EXPO_PUBLIC_SANITY_DATASET=production

# ---------------------------------------------------------------------------
# Legal & support links (Parent Area → "About & Legal")
# ---------------------------------------------------------------------------
# Public URLs, never credentials, so EXPO_PUBLIC_ is the right home for them.
#
# Each row renders only when its value is set AND parses as an allowed scheme
# (https/http; support also accepts mailto:). Anything unset or malformed is
# treated as absent and the row is simply not shown — there are no dead links.
#
# ⚠️ Google Play requires a privacy policy for this app. The release-readiness
# check (application/release/releaseReadiness.ts) reports BLOCKED while
# EXPO_PUBLIC_LEGAL_PRIVACY_POLICY_URL is unset, so set it before uploading a
# Play candidate. It is deliberately a test/tooling failure, not a runtime
# crash: a missing marketing URL must never stop a child opening the app.
# EXPO_PUBLIC_LEGAL_PRIVACY_POLICY_URL=https://example.com/factnuggets/privacy
# EXPO_PUBLIC_LEGAL_TERMS_URL=https://example.com/factnuggets/terms
# EXPO_PUBLIC_LEGAL_SUPPORT_URL=https://example.com/factnuggets/support
